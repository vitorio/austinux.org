grow_version: ">=0.0.60"
project: austinux
home: /content/pages/home.md

localization:
  default_locale: en

title: "Austin UX"

static_dirs:
- static_dir: /files/oldfiles/
  serve_at: /

deployments:
  s3:
    destination: s3
    bucket: austinux.org
    env:
      host: austinux.org
